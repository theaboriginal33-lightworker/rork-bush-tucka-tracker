# Docs images

Place optional screenshots here for client handoffs:

- **`supabase-dashboard.png`** — Supabase: **Table Editor** (e.g. `scan_journal_entries`, `cookbook_manual_entries`) + **Storage → `scan-images`**.  
  After saving, you can embed in `SUPABASE_CLIENT_UPDATE.md` with:

  `![Supabase dashboard](images/supabase-dashboard.png)`
